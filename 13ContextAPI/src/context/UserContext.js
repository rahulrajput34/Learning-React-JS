// Step 1: create the contexts

import React from "react";

// we can also give default values inside this context as well as we give inside useState 
const UsContext = React.createContext();

export default UsContext;